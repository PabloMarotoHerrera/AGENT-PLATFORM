# Skeleton Boundary

This file is placeholder only, no runtime, no implementation logic, no dependency adoption, no provider/API/MCP activation, no product activation, no CSS substrate decision.

## Allowed Files
Allowed files are the exact I-00 Markdown skeleton files under `3_platform/_governed_skeleton/` and no others.

## Forbidden Files
Forbidden files include code, package manifests, lockfiles, scripts, tests, tools, CI, hooks, schemas, registries, databases, graph artifacts, ontology artifacts, vector artifacts, runtime modules, provider configs, adapter configs, MCP configs, and product source.

## No Code
The skeleton contains no executable source code and no implementation logic.

## No Dependencies
The skeleton adopts no dependencies, creates no package manifests, creates no lockfiles, and provides no install instructions.

## No Runtime Or Execution
The skeleton creates no runtime, agents, context engine, validation registry, security enforcement, executable tools, tests, builds, scripts, solvers, SDK execution, native binaries, products, providers, APIs, MCP, network calls, or authentication flows.

## No Product Source
Product source remains local-only and is not copied into this skeleton.

## No Local-only Leakage
Secrets, credentials, datasets, models, artifacts, generated outputs, product source, external source, and local-only workspaces are not included.

## No Activation Or Substrate Decision
Provider/API/MCP activation remains blocked. Cognitive Semantic System substrate remains deferred. Product activation remains blocked.

## No Broad Approval
This skeleton is not broad implementation approval, not broad source tracking approval, and not approval of existing `3_platform` contents.

## Stop Rules
Stop if a request requires files outside exact I-00 scope, existing `3_platform` inspection, code, packages, dependencies, execution, providers, APIs, MCP, products, CSS substrate selection, security enforcement, validation registry, Git mutation, or I-01 start.
